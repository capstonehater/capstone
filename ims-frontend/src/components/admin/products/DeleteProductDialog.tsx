"use client";
import { X } from "lucide-react";
import type { DeleteEligibility, ProductDetail } from "@/lib/products";
type Props={open:boolean;product:ProductDetail|null;submitting:boolean;eligibility:DeleteEligibility|null;loadingEligibility:boolean;errorMessage?:string|null;onClose:()=>void;onConfirm:()=>Promise<void>};
export default function DeleteProductDialog({open,product,submitting,eligibility,loadingEligibility,errorMessage,onClose,onConfirm}:Props){if(!open||!product)return null;const e=eligibility??product.deleteEligibility;return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/60 p-4"><div className="w-full max-w-lg overflow-hidden rounded-xl bg-white shadow-2xl"><header className="flex items-start justify-between border-b border-slate-200 px-6 py-5"><div><h2 className="text-xl font-bold text-slate-900">Delete Product</h2><p className="mt-1 text-sm text-slate-500">Permanently remove this product if deletion is allowed.</p></div><button onClick={onClose} className="rounded p-1 text-slate-500 hover:bg-slate-100"><X className="h-5 w-5"/></button></header><div className="space-y-4 p-6"><div className="rounded-md border border-red-200 bg-red-50 p-3 text-sm text-red-800">Permanent deletion cannot be undone.</div><div className="rounded-md border border-slate-200 bg-slate-50 p-4 text-sm"><p><b>Product:</b> {product.name}</p><p><b>SKU/Product Code:</b> {product.variants[0]?.sku ?? "—"}</p><p><b>Category:</b> {product.category.name}</p><p><b>Variants:</b> {product.variantCount} &nbsp; <b>Ingredients:</b> {product.ingredientCount}</p><p><b>Lifecycle:</b> {product.archive.state}</p></div><div className="rounded-md border border-slate-200 p-4 text-sm">{loadingEligibility?"Checking deletion status...":e.eligible?<span className="text-green-700">Deletion is allowed for this product.</span>:<><p className="font-semibold text-slate-800">Cannot delete because existing history or dependencies exist.</p><ul className="mt-2 list-disc pl-5 text-slate-600">{e.blockingReasons.map(r=><li key={r.code}>{r.message} ({r.count})</li>)}</ul></>}{errorMessage?<p className="mt-2 text-red-700">{errorMessage}</p>:null}</div><footer className="flex justify-end gap-3 border-t border-slate-200 pt-4"><button onClick={onClose} className="rounded-md border border-slate-300 px-5 py-2 text-sm font-semibold text-slate-700">Cancel</button><button onClick={()=>void onConfirm()} disabled={submitting||loadingEligibility||!e.eligible} className="rounded-md bg-red-600 px-5 py-2 text-sm font-semibold text-white disabled:opacity-50">{submitting?"Deleting...":"Delete Product"}</button></footer></div></div></div>}
